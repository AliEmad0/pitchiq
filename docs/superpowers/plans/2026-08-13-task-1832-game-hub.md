# TASK-1832 — The Game Hub Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn `/game` from a fixed broadcast demo into the mode-selection gate, put it in the header nav and sitemap, end the `/game/draft` ↔ `/game/play` duplication, and upgrade the pre-match screen.

**Architecture:** One pure-data registry (`domain/modes.ts`) drives a client gate component. Every mode declares a status _per format_ (`single` / `season`); the gate reads the flag and never branches on a mode's identity, so unlocking a mode later is a data change. No new data loading — the gate imports nothing but the registry and i18n.

**Tech Stack:** Next 15 App Router, React 19, TypeScript, Tailwind v4, next-intl (en + ar/RTL), Vitest + Testing Library, Playwright.

**Spec:** [`docs/superpowers/specs/2026-08-13-task-1832-game-hub-design.md`](../specs/2026-08-13-task-1832-game-hub-design.md)

**Branch:** `feat/task-1832-game-hub` (already created, spec committed at `c4e0268`)

---

## Before you start — environment

Every `pnpm` / `node` command **must** run through WSL with nvm sourced. From a Windows shell:

```bash
wsl -d Ubuntu -- bash -lc 'source $HOME/.nvm/nvm.sh && nvm use 22 >/dev/null && cd ~/projects/pitchiq-1807a && pnpm test'
```

Commands below are written bare (`pnpm test …`) — wrap each one in that prefix.

**Three rules that will bite you if ignored:**

1. **Commit from WSL with nvm sourced**, or the husky pre-commit hook fails with `exec: node: not found` — the Windows pnpm shim resolves first and cannot find node.
2. **Never `git add -A`.** This repo is shared with other worktrees. Stage explicit paths only.
3. **`pnpm lint` needs `CI=true`** or pnpm 11 aborts with `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`.

**Verification rule for this plan:** every new guard test must be seen to **fail** before you trust it. Vitest does not type-check — run `pnpm type-check` before claiming any task done.

---

## File structure

| File                                              | Responsibility                                                                                |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `src/features/game/domain/modes.ts`               | **New.** The registry: mode rows, collection rows, types. Pure data, no imports beyond types. |
| `src/features/game/components/ModeTile.tsx`       | **New.** One mode — live (a button) or locked (inert).                                        |
| `src/features/game/components/FormatChoice.tsx`   | **New.** The One Match / Full Season pair inside an expanded tile.                            |
| `src/features/game/components/ModeGate.tsx`       | **New.** Composes groups + tiles; owns which tile is open.                                    |
| `src/features/game/components/MiniPitch.tsx`      | **New.** Read-only formation render for the pre-match VS screen.                              |
| `src/app/[locale]/game/page.tsx`                  | **Rewrite.** Renders `<ModeGate />`. Loads no data.                                           |
| `src/app/[locale]/game/demo/page.tsx`             | **New.** Today's `/game` body, verbatim.                                                      |
| `src/app/[locale]/game/play/page.tsx`             | **Delete.** Replaced by a `next.config.ts` redirect.                                          |
| `src/features/game/components/MatchupPreview.tsx` | **Modify.** Side-by-side XIs + impact lines.                                                  |
| `src/features/game/components/GamePlay.tsx`       | **Modify.** Pass both teams to `MatchupPreview`.                                              |
| `src/components/layout/nav-items.ts`              | **Modify.** `/game` into `NAV_ITEMS` + `PRIMARY_NAV_HREFS`.                                   |
| `src/app/sitemap.ts`                              | **Modify.** `/game` + `ar` alternate.                                                         |
| `next.config.ts`                                  | **Modify.** `/game/play` → `/game/draft` redirect, both locales.                              |
| `scripts/warm-e2e-routes.sh`                      | **Modify.** `+/game/demo`, `−/game/play`.                                                     |
| `src/i18n/messages/en.json`, `ar.json`            | **Modify.** ~40 new `game` keys + `nav.game`.                                                 |

---

## Task 1: The mode registry

**Files:**

- Create: `src/features/game/domain/modes.ts`
- Test: `tests/unit/game-modes.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
import { readdirSync, statSync } from "node:fs";
import { join } from "node:path";
import { describe, expect, it } from "vitest";
import en from "@/i18n/messages/en.json";
import ar from "@/i18n/messages/ar.json";
import { COLLECTION_SURFACES, GAME_MODES, MODE_GROUPS } from "@/features/game/domain/modes";

/** Every `page.tsx` under app/[locale]/game, as a route path like "/game/chaos". */
function gameRoutes(): string[] {
  const root = join(process.cwd(), "src", "app", "[locale]", "game");
  const out: string[] = [];
  const walk = (dir: string, prefix: string) => {
    for (const entry of readdirSync(dir)) {
      const full = join(dir, entry);
      if (statSync(full).isDirectory()) walk(full, `${prefix}/${entry}`);
      else if (entry === "page.tsx") out.push(prefix);
    }
  };
  walk(root, "/game");
  return out;
}

describe("the mode registry", () => {
  it("has unique ids", () => {
    const ids = GAME_MODES.map((m) => m.id);
    expect(new Set(ids).size).toBe(ids.length);
  });

  it("gives every live format somewhere to go", () => {
    for (const mode of GAME_MODES) {
      const anyLive = Object.values(mode.formats).some((s) => s === "live");
      if (anyLive) expect(mode.href, `${mode.id} is live but has no href`).not.toBeNull();
    }
  });

  it("points every href at a route that exists", () => {
    const routes = gameRoutes();
    for (const mode of GAME_MODES) {
      if (mode.href == null) continue;
      expect(routes, `${mode.id} -> ${mode.href}`).toContain(mode.href);
    }
  });

  it("resolves every label key in BOTH locales", () => {
    const keys = [
      ...GAME_MODES.flatMap((m) => [m.nameKey, m.descriptionKey]),
      ...COLLECTION_SURFACES.map((c) => c.nameKey),
      ...MODE_GROUPS.map((g) => g.labelKey),
    ];
    for (const key of keys) {
      expect(en.game, `en.game.${key}`).toHaveProperty(key);
      expect(ar.game, `ar.game.${key}`).toHaveProperty(key);
    }
  });

  it("assigns every mode to a declared group", () => {
    const groups = new Set(MODE_GROUPS.map((g) => g.id));
    for (const mode of GAME_MODES) expect(groups).toContain(mode.group);
  });
});
```

- [ ] **Step 2: Run it and watch it fail**

Run: `pnpm test tests/unit/game-modes.test.ts`
Expected: FAIL — `Failed to resolve import "@/features/game/domain/modes"`.

- [ ] **Step 3: Write the registry**

Create `src/features/game/domain/modes.ts`:

```ts
/**
 * The game's mode roster — pure data, no imports, no entropy.
 *
 * ⚠️ Phase 18 locked "the modes are rule packs (data), not code paths". This file is the
 * UI half of that rule: the gate renders from this table and never branches on a mode's
 * identity. Shipping a mode later flips a status and sets an href; no component changes.
 *
 * ⚠️ Labels are i18n KEYS, never literals. The CI AST guard rejects hardcoded strings in
 * features/game, and a literal here would ship English into the Arabic gate.
 */

export type ModeId =
  | "h2h"
  | "chaos"
  | "captains"
  | "budget"
  | "chemistry"
  | "legacy"
  | "classic"
  | "daily"
  | "weekly"
  | "whatIf"
  | "mystery";

/** How long a session runs. Orthogonal to the mode (TASK-1832 D5). */
export type GameFormat = "single" | "season";

export type ModeStatus = "live" | "planned";

export type ModeGroupId = "quickPlay" | "draftPacks" | "challenges";

export interface GameMode {
  id: ModeId;
  group: ModeGroupId;
  emoji: string;
  nameKey: string;
  descriptionKey: string;
  /**
   * The mode's entry route. One route serves every live format — the format does not
   * change where you land today (D11: no `?format=` param). `null` while every format is
   * `planned`.
   */
  href: string | null;
  formats: Record<GameFormat, ModeStatus>;
  /** The ticket that makes this mode live. Documentation only. */
  ticket: string;
}

export interface ModeGroup {
  id: ModeGroupId;
  labelKey: string;
}

export interface CollectionSurface {
  id: string;
  emoji: string;
  nameKey: string;
  status: ModeStatus;
  ticket: string;
}

export const MODE_GROUPS: readonly ModeGroup[] = [
  { id: "quickPlay", labelKey: "groupQuickPlay" },
  { id: "draftPacks", labelKey: "groupDraftPacks" },
  { id: "challenges", labelKey: "groupChallenges" },
];

const planned: Record<GameFormat, ModeStatus> = { single: "planned", season: "planned" };

export const GAME_MODES: readonly GameMode[] = [
  {
    id: "h2h",
    group: "quickPlay",
    emoji: "🧠",
    nameKey: "modeH2hName",
    descriptionKey: "modeH2hDesc",
    href: "/game/draft",
    formats: { single: "live", season: "planned" },
    ticket: "TASK-1807",
  },
  {
    id: "chaos",
    group: "quickPlay",
    emoji: "🔥",
    nameKey: "modeChaosName",
    descriptionKey: "modeChaosDesc",
    href: "/game/chaos",
    formats: { single: "live", season: "planned" },
    ticket: "TASK-1806",
  },
  {
    id: "captains",
    group: "draftPacks",
    emoji: "👑",
    nameKey: "modeCaptainsName",
    descriptionKey: "modeCaptainsDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1810",
  },
  {
    id: "budget",
    group: "draftPacks",
    emoji: "💰",
    nameKey: "modeBudgetName",
    descriptionKey: "modeBudgetDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1810",
  },
  {
    id: "chemistry",
    group: "draftPacks",
    emoji: "🔗",
    nameKey: "modeChemistryName",
    descriptionKey: "modeChemistryDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1810",
  },
  {
    id: "legacy",
    group: "draftPacks",
    emoji: "🏛️",
    nameKey: "modeLegacyName",
    descriptionKey: "modeLegacyDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1810",
  },
  {
    // ⚠️ "Classic", NOT "Classic Season" — the format decides length (D7). Survival is
    // deliberately absent: it is an OBJECTIVE on the Season format, under TASK-1811.
    id: "classic",
    group: "draftPacks",
    emoji: "🎩",
    nameKey: "modeClassicName",
    descriptionKey: "modeClassicDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1810",
  },
  {
    id: "daily",
    group: "challenges",
    emoji: "📅",
    nameKey: "modeDailyName",
    descriptionKey: "modeDailyDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1817",
  },
  {
    id: "weekly",
    group: "challenges",
    emoji: "🗓️",
    nameKey: "modeWeeklyName",
    descriptionKey: "modeWeeklyDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1828",
  },
  {
    id: "whatIf",
    group: "challenges",
    emoji: "⏳",
    nameKey: "modeWhatIfName",
    descriptionKey: "modeWhatIfDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1816",
  },
  {
    id: "mystery",
    group: "challenges",
    emoji: "🎲",
    nameKey: "modeMysteryName",
    descriptionKey: "modeMysteryDesc",
    href: null,
    formats: planned,
    ticket: "TASK-1818",
  },
];

/** Not modes — they render in their own strip at the foot of the gate. */
export const COLLECTION_SURFACES: readonly CollectionSurface[] = [
  {
    id: "records",
    emoji: "📊",
    nameKey: "collectionRecords",
    status: "planned",
    ticket: "TASK-1812",
  },
  {
    id: "hallOfFame",
    emoji: "🥇",
    nameKey: "collectionHallOfFame",
    status: "planned",
    ticket: "TASK-1813",
  },
  { id: "album", emoji: "🖼️", nameKey: "collectionAlbum", status: "planned", ticket: "TASK-1819" },
];

/** The formats a tile offers, in display order. */
export const GAME_FORMATS: readonly GameFormat[] = ["single", "season"];

export const isPlayable = (mode: GameMode): boolean =>
  mode.href != null && Object.values(mode.formats).some((s) => s === "live");

export const modesInGroup = (group: ModeGroupId): GameMode[] =>
  GAME_MODES.filter((m) => m.group === group);
```

- [ ] **Step 4: Run it — the i18n assertion should still fail**

Run: `pnpm test tests/unit/game-modes.test.ts`
Expected: 4 pass, 1 FAIL — "resolves every label key in BOTH locales" (keys land in Task 2). This is the correct intermediate state; do not add keys yet.

- [ ] **Step 5: Commit**

```bash
git add src/features/game/domain/modes.ts tests/unit/game-modes.test.ts
git commit -m "feat(game): add the mode registry that drives the hub gate"
```

Commit with the i18n test red — Task 2 turns it green. If you would rather not commit red, do Tasks 1 and 2 back-to-back and commit once.

---

## Task 2: The i18n keys

**Files:**

- Modify: `src/i18n/messages/en.json`, `src/i18n/messages/ar.json`

- [ ] **Step 1: Add the English keys**

Add to the `game` object in `src/i18n/messages/en.json`:

```json
"hubTitle": "Play PitchIQ",
"hubSubtitle": "Thirty-four seasons of real players. Draft an XI and see what happens.",
"sectionPlayNow": "Play now",
"sectionComingSoon": "Coming soon",
"groupQuickPlay": "Quick play",
"groupDraftPacks": "Draft packs",
"groupChallenges": "Challenges",
"groupCollection": "Your collection",
"formatSingle": "One Match",
"formatSingleHint": "About three minutes",
"formatSeason": "Full Season",
"formatSeasonHint": "38 matchweeks",
"statusPlanned": "In development",
"modeH2hName": "Tactical H2H",
"modeH2hDesc": "Draft an XI. Call the shots. Win.",
"modeChaosName": "Chaos Draft",
"modeChaosDesc": "A random XI and an instant match.",
"modeCaptainsName": "Captain's Draft",
"modeCaptainsDesc": "Pick an icon first, then build around them.",
"modeBudgetName": "Budget Cap Draft",
"modeBudgetDesc": "Build the best XI you can afford.",
"modeChemistryName": "Chemistry Draft",
"modeChemistryDesc": "Link players by club, nation and season.",
"modeLegacyName": "Legacy Club",
"modeLegacyDesc": "One club, drafted season by season.",
"modeClassicName": "Classic",
"modeClassicDesc": "Real clubs, real squads, real records.",
"modeDailyName": "Daily Challenge",
"modeDailyDesc": "One fixture a day. Same for everyone.",
"modeWeeklyName": "Weekly Ladder",
"modeWeeklyDesc": "A rotating ruleset that changes every week.",
"modeWhatIfName": "What-If Scenarios",
"modeWhatIfDesc": "Step into a famous match and rewrite it.",
"modeMysteryName": "Mystery Market",
"modeMysteryDesc": "Survive a run. Three losses and it's over.",
"collectionRecords": "Records",
"collectionHallOfFame": "Hall of Fame",
"collectionAlbum": "Sticker Album",
"modeLockedAria": "{name} — in development",
"gateAria": "Choose a game mode"
```

Add to the `nav` object in the same file:

```json
"game": "Game"
```

- [ ] **Step 2: Add the Arabic keys**

Add the same key set to `src/i18n/messages/ar.json` under `game`, and `"game": "اللعبة"` under `nav`:

```json
"hubTitle": "العب PitchIQ",
"hubSubtitle": "أربعة وثلاثون موسمًا من اللاعبين الحقيقيين. اختر تشكيلتك وشاهد ما يحدث.",
"sectionPlayNow": "العب الآن",
"sectionComingSoon": "قريبًا",
"groupQuickPlay": "لعب سريع",
"groupDraftPacks": "حزم الاختيار",
"groupChallenges": "التحديات",
"groupCollection": "مجموعتك",
"formatSingle": "مباراة واحدة",
"formatSingleHint": "نحو ثلاث دقائق",
"formatSeason": "موسم كامل",
"formatSeasonHint": "٣٨ جولة",
"statusPlanned": "قيد التطوير",
"modeH2hName": "مواجهة تكتيكية",
"modeH2hDesc": "اختر تشكيلتك، واتخذ القرارات، وانتصر.",
"modeChaosName": "اختيار عشوائي",
"modeChaosDesc": "تشكيلة عشوائية ومباراة فورية.",
"modeCaptainsName": "اختيار القائد",
"modeCaptainsDesc": "اختر نجمًا أولًا ثم ابنِ حوله.",
"modeBudgetName": "اختيار بميزانية",
"modeBudgetDesc": "أفضل تشكيلة تقدر على تحمّل تكلفتها.",
"modeChemistryName": "اختيار الانسجام",
"modeChemistryDesc": "اربط اللاعبين بالنادي والجنسية والموسم.",
"modeLegacyName": "إرث النادي",
"modeLegacyDesc": "نادٍ واحد، موسمًا بعد موسم.",
"modeClassicName": "كلاسيكي",
"modeClassicDesc": "أندية حقيقية وتشكيلات وسجلات حقيقية.",
"modeDailyName": "تحدي اليوم",
"modeDailyDesc": "مباراة واحدة يوميًا، نفسها للجميع.",
"modeWeeklyName": "سلّم أسبوعي",
"modeWeeklyDesc": "قواعد متغيرة كل أسبوع.",
"modeWhatIfName": "ماذا لو",
"modeWhatIfDesc": "ادخل مباراة شهيرة وأعد كتابتها.",
"modeMysteryName": "السوق الغامض",
"modeMysteryDesc": "انجُ بأطول سلسلة. ثلاث هزائم وينتهي كل شيء.",
"collectionRecords": "السجلات",
"collectionHallOfFame": "قاعة المشاهير",
"collectionAlbum": "ألبوم الملصقات",
"modeLockedAria": "{name} — قيد التطوير",
"gateAria": "اختر نمط اللعب"
```

- [ ] **Step 3: Run the registry test — now fully green**

Run: `pnpm test tests/unit/game-modes.test.ts`
Expected: PASS, 5/5.

- [ ] **Step 4: Prove the i18n guard actually guards**

Temporarily delete `"modeClassicName"` from `ar.json`, re-run, and confirm it FAILS naming `ar.game.modeClassicName`. Restore it. A guard you have not seen fail is not a guard.

- [ ] **Step 5: Run the full i18n parity suite**

Run: `pnpm test tests/unit/i18n`
Expected: PASS — en and ar stay structurally identical.

- [ ] **Step 6: Commit**

```bash
git add src/i18n/messages/en.json src/i18n/messages/ar.json
git commit -m "feat(game): add hub gate strings in English and Arabic"
```

---

## Task 3: `ModeTile` and `FormatChoice`

**Files:**

- Create: `src/features/game/components/ModeTile.tsx`, `src/features/game/components/FormatChoice.tsx`
- Test: `tests/unit/game-mode-tile.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
import { screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import { GAME_MODES } from "@/features/game/domain/modes";
import { renderWithIntl } from "./_helpers/intl";

const { ModeTile } = await import("@/features/game/components/ModeTile");

const h2h = GAME_MODES.find((m) => m.id === "h2h")!;
const captains = GAME_MODES.find((m) => m.id === "captains")!;

describe("ModeTile", () => {
  it("renders a live mode as a button", () => {
    renderWithIntl(<ModeTile mode={h2h} open={false} onOpen={vi.fn()} />);
    expect(screen.getByRole("button", { name: /Tactical H2H/ })).toBeInTheDocument();
  });

  it("calls onOpen when a live tile is clicked", async () => {
    const onOpen = vi.fn();
    renderWithIntl(<ModeTile mode={h2h} open={false} onOpen={onOpen} />);
    await userEvent.click(screen.getByRole("button", { name: /Tactical H2H/ }));
    expect(onOpen).toHaveBeenCalledWith("h2h");
  });

  it("shows the formats only when open", async () => {
    const { rerender } = renderWithIntl(<ModeTile mode={h2h} open={false} onOpen={vi.fn()} />);
    expect(screen.queryByRole("link", { name: /One Match/ })).not.toBeInTheDocument();
    rerender(<ModeTile mode={h2h} open onOpen={vi.fn()} />);
    expect(await screen.findByRole("link", { name: /One Match/ })).toBeInTheDocument();
  });

  it("renders a planned mode as NOT focusable", () => {
    renderWithIntl(<ModeTile mode={captains} open={false} onOpen={vi.fn()} />);
    // Visible…
    expect(screen.getByText("Captain's Draft")).toBeInTheDocument();
    // …but never a control, or eleven locked tiles become eleven dead tab stops.
    expect(screen.queryByRole("button", { name: /Captain's Draft/ })).not.toBeInTheDocument();
    expect(screen.getAllByText("In development").length).toBeGreaterThan(0);
  });

  it("renders a planned FORMAT as text, not a link", () => {
    renderWithIntl(<ModeTile mode={h2h} open onOpen={vi.fn()} />);
    expect(screen.getByRole("link", { name: /One Match/ })).toBeInTheDocument();
    expect(screen.queryByRole("link", { name: /Full Season/ })).not.toBeInTheDocument();
    expect(screen.getByText("Full Season")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run it and watch it fail**

Run: `pnpm test tests/unit/game-mode-tile.test.tsx`
Expected: FAIL — cannot resolve `@/features/game/components/ModeTile`.

- [ ] **Step 3: Write `FormatChoice`**

Create `src/features/game/components/FormatChoice.tsx`:

```tsx
"use client";
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
import { GAME_FORMATS, type GameMode } from "@/features/game/domain/modes";

const LABEL_KEY = { single: "formatSingle", season: "formatSeason" } as const;
const HINT_KEY = { single: "formatSingleHint", season: "formatSeasonHint" } as const;

/**
 * The One Match / Full Season pair inside an expanded tile.
 *
 * ⚠️ A `planned` format renders as TEXT, never a disabled control. Day one that is every
 * mode's `season` (the 38-week engine is TASK-1810/1811), so a disabled button here would
 * put a dead tab stop under every playable mode.
 */
export function FormatChoice({ mode }: { mode: GameMode }) {
  const t = useTranslations("game");

  return (
    <div className="mt-3 flex gap-2">
      {GAME_FORMATS.map((format) => {
        const label = t(LABEL_KEY[format]);
        const hint = t(HINT_KEY[format]);
        const live = mode.formats[format] === "live" && mode.href != null;

        if (!live) {
          return (
            <div
              key={format}
              className="border-border flex-1 rounded-md border border-dashed p-2 text-center opacity-45"
            >
              <span className="block text-xs font-bold">{label}</span>
              <span className="block text-[10px]">🔒 {t("statusPlanned")}</span>
            </div>
          );
        }

        return (
          <Link
            key={format}
            href={mode.href!}
            className="border-primary/70 flex-1 rounded-md border p-2 text-center"
          >
            <span className="block text-xs font-bold">{label}</span>
            <span className="text-muted-foreground block text-[10px]">{hint}</span>
          </Link>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 4: Write `ModeTile`**

Create `src/features/game/components/ModeTile.tsx`:

```tsx
"use client";
import { useTranslations } from "next-intl";
import { isPlayable, type GameMode, type ModeId } from "@/features/game/domain/modes";
import { FormatChoice } from "./FormatChoice";

interface Props {
  mode: GameMode;
  open: boolean;
  onOpen: (id: ModeId) => void;
}

/**
 * One mode on the gate.
 *
 * ⚠️ A `planned` mode is NOT a disabled button — it is not a control at all. Eleven
 * disabled buttons would be eleven dead stops in the tab order. It stays perceivable via
 * a visible "In development" label.
 */
export function ModeTile({ mode, open, onOpen }: Props) {
  const t = useTranslations("game");
  const name = t(mode.nameKey);

  if (!isPlayable(mode)) {
    return (
      <div
        className="border-border rounded-full border border-dashed px-3 py-1 text-xs opacity-45"
        aria-label={t("modeLockedAria", { name })}
      >
        <span aria-hidden>{mode.emoji}</span> {name}
        <span className="sr-only">{t("statusPlanned")}</span>
        <span aria-hidden className="ms-1 opacity-70">
          {t("statusPlanned")}
        </span>
      </div>
    );
  }

  return (
    <div className={`border-border rounded-lg border p-3 ${open ? "border-primary/80" : ""}`}>
      <button
        type="button"
        aria-expanded={open}
        onClick={() => onOpen(mode.id)}
        className="w-full text-start"
      >
        <span className="block text-xl" aria-hidden>
          {mode.emoji}
        </span>
        <span className="mt-1 block text-sm font-bold">{name}</span>
        <span className="text-muted-foreground block text-xs">{t(mode.descriptionKey)}</span>
      </button>
      {open ? <FormatChoice mode={mode} /> : null}
    </div>
  );
}
```

- [ ] **Step 5: Run the test**

Run: `pnpm test tests/unit/game-mode-tile.test.tsx`
Expected: PASS, 5/5.

- [ ] **Step 6: Prove the accessibility guard bites**

Temporarily change the locked branch to render `<button disabled>`. Re-run: the "renders a planned mode as NOT focusable" case must FAIL. Revert.

- [ ] **Step 7: Commit**

```bash
git add src/features/game/components/ModeTile.tsx src/features/game/components/FormatChoice.tsx tests/unit/game-mode-tile.test.tsx
git commit -m "feat(game): add the mode tile and its format choice"
```

---

## Task 4: `ModeGate`

**Files:**

- Create: `src/features/game/components/ModeGate.tsx`
- Test: `tests/unit/game-mode-gate.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
import { screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it } from "vitest";
import { COLLECTION_SURFACES, GAME_MODES } from "@/features/game/domain/modes";
import { renderWithIntl } from "./_helpers/intl";
import en from "@/i18n/messages/en.json";

const { ModeGate } = await import("@/features/game/components/ModeGate");

describe("ModeGate", () => {
  it("renders every mode in the registry exactly once", () => {
    renderWithIntl(<ModeGate />);
    for (const mode of GAME_MODES) {
      const name = (en.game as Record<string, string>)[mode.nameKey];
      expect(screen.getAllByText(name), name).toHaveLength(1);
    }
  });

  it("renders every collection surface", () => {
    renderWithIntl(<ModeGate />);
    for (const surface of COLLECTION_SURFACES) {
      const name = (en.game as Record<string, string>)[surface.nameKey];
      expect(screen.getByText(name)).toBeInTheDocument();
    }
  });

  it("opens a mode on click and collapses it on a second click", async () => {
    renderWithIntl(<ModeGate />);
    const tile = screen.getByRole("button", { name: /Tactical H2H/ });

    expect(tile).toHaveAttribute("aria-expanded", "false");
    await userEvent.click(tile);
    expect(tile).toHaveAttribute("aria-expanded", "true");
    expect(screen.getByRole("link", { name: /One Match/ })).toBeInTheDocument();

    await userEvent.click(tile);
    expect(tile).toHaveAttribute("aria-expanded", "false");
    expect(screen.queryByRole("link", { name: /One Match/ })).not.toBeInTheDocument();
  });

  it("keeps only one mode open at a time", async () => {
    renderWithIntl(<ModeGate />);
    const h2h = screen.getByRole("button", { name: /Tactical H2H/ });
    const chaos = screen.getByRole("button", { name: /Chaos Draft/ });

    await userEvent.click(h2h);
    await userEvent.click(chaos);

    expect(h2h).toHaveAttribute("aria-expanded", "false");
    expect(chaos).toHaveAttribute("aria-expanded", "true");
  });

  it("exposes no control for any planned mode", () => {
    renderWithIntl(<ModeGate />);
    // Exactly two playable modes today: H2H and Chaos.
    const tiles = screen.getAllByRole("button").filter((b) => b.hasAttribute("aria-expanded"));
    expect(tiles).toHaveLength(2);
  });
});
```

- [ ] **Step 2: Run it and watch it fail**

Run: `pnpm test tests/unit/game-mode-gate.test.tsx`
Expected: FAIL — cannot resolve `ModeGate`.

- [ ] **Step 3: Write `ModeGate`**

Create `src/features/game/components/ModeGate.tsx`:

```tsx
"use client";
import { useTranslations } from "next-intl";
import { useState } from "react";
import {
  COLLECTION_SURFACES,
  GAME_MODES,
  MODE_GROUPS,
  isPlayable,
  modesInGroup,
  type ModeId,
} from "@/features/game/domain/modes";
import { ModeTile } from "./ModeTile";

/**
 * The gate: every mode the game has or will have, and the way into the two that work.
 *
 * ⚠️ Renders entirely from `domain/modes.ts`. Nothing here knows what "Season" is — a
 * mode's status drives everything, so unlocking one later is a data change (TASK-1832 D5).
 *
 * ⚠️ Loads NO data and imports no `adapter/*`. That is what keeps `/game` trivially
 * static, and it is why the eventual redesign (D10) can replace this file wholesale.
 *
 * One `useState` rather than a reducer: there is a single piece of state and no illegal
 * transition to guard, unlike `draft-state` / `room-state` / `play-machine`.
 */
export function ModeGate() {
  const t = useTranslations("game");
  const [open, setOpen] = useState<ModeId | null>(null);

  const playable = GAME_MODES.filter(isPlayable);

  return (
    <div className="mx-auto w-full max-w-5xl" aria-label={t("gateAria")}>
      <h1 className="text-2xl font-extrabold tracking-tight">{t("hubTitle")}</h1>
      <p className="text-muted-foreground mt-1 mb-6 text-sm">{t("hubSubtitle")}</p>

      <h2 className="text-muted-foreground mb-2 font-mono text-[10px] font-bold tracking-widest uppercase">
        {t("sectionPlayNow")}
      </h2>
      <div className="mb-7 flex flex-wrap items-start gap-3">
        {playable.map((mode) => (
          <div key={mode.id} className="min-w-[200px] flex-1">
            <ModeTile
              mode={mode}
              open={open === mode.id}
              onOpen={(id) => setOpen((prior) => (prior === id ? null : id))}
            />
          </div>
        ))}
      </div>

      <h2 className="text-muted-foreground mb-2 font-mono text-[10px] font-bold tracking-widest uppercase">
        {t("sectionComingSoon")}
      </h2>
      {MODE_GROUPS.map((group) => {
        const locked = modesInGroup(group.id).filter((m) => !isPlayable(m));
        if (locked.length === 0) return null;
        return (
          <div key={group.id} className="mb-3">
            <h3 className="text-muted-foreground mb-1.5 text-[10px] font-semibold uppercase opacity-70">
              {t(group.labelKey)}
            </h3>
            <div className="flex flex-wrap gap-1.5">
              {locked.map((mode) => (
                <ModeTile key={mode.id} mode={mode} open={false} onOpen={() => {}} />
              ))}
            </div>
          </div>
        );
      })}

      <div className="border-border mt-6 border-t pt-4">
        <h3 className="text-muted-foreground mb-1.5 text-[10px] font-semibold uppercase opacity-70">
          {t("groupCollection")}
        </h3>
        <div className="flex flex-wrap gap-1.5">
          {COLLECTION_SURFACES.map((surface) => (
            <div
              key={surface.id}
              className="border-border rounded-full border border-dashed px-3 py-1 text-xs opacity-45"
            >
              <span aria-hidden>{surface.emoji}</span> {t(surface.nameKey)}
              <span aria-hidden className="ms-1 opacity-70">
                {t("statusPlanned")}
              </span>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Run the test**

Run: `pnpm test tests/unit/game-mode-gate.test.tsx`
Expected: PASS, 5/5.

- [ ] **Step 5: Commit**

```bash
git add src/features/game/components/ModeGate.tsx tests/unit/game-mode-gate.test.tsx
git commit -m "feat(game): add the mode gate that renders the whole roster"
```

---

## Task 5: Move the demo, rewrite `/game`

**Files:**

- Create: `src/app/[locale]/game/demo/page.tsx`
- Modify: `src/app/[locale]/game/page.tsx` (full rewrite)
- Modify: `tests/unit/game-routes-static.test.ts`

- [ ] **Step 1: Create the demo route with today's `/game` body**

Create `src/app/[locale]/game/demo/page.tsx` — this is the CURRENT contents of `src/app/[locale]/game/page.tsx` with only the metadata keys changed:

```tsx
import type { Metadata } from "next";
import { getTranslations, setRequestLocale } from "next-intl/server";
import { assembleGameTeam } from "@/features/game/adapter/lineup";
import { loadSeasonGoalRate } from "@/features/game/adapter/match";
import { MatchView } from "@/features/game/components/MatchView";
import { simulate } from "@/features/game/domain/simulate";
import { buildMatchViewModel } from "@/features/game/view/match-view-model";

export const dynamic = "force-static";
export const revalidate = 86400;

const HOME_ID = 42; // Arsenal
const AWAY_ID = 33; // Manchester United
const SEASON = 2020;
const SEED = 20040515;

type Props = { params: Promise<{ locale: string }> };

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params;
  setRequestLocale(locale);
  const t = await getTranslations("game");
  return { title: t("title"), description: t("subtitle") };
}

export default async function GameDemoPage({ params }: Props) {
  const { locale } = await params;
  setRequestLocale(locale);
  const t = await getTranslations("game");

  const [home, away, rate] = await Promise.all([
    assembleGameTeam(HOME_ID, SEASON),
    assembleGameTeam(AWAY_ID, SEASON),
    loadSeasonGoalRate(SEASON),
  ]);

  return (
    <main className="mx-auto w-full max-w-4xl px-4 py-10">
      <h1 className="text-2xl font-extrabold tracking-tight">{t("title")}</h1>
      <p className="text-muted-foreground mb-8 mt-1 text-sm">{t("subtitle")}</p>
      {home && away ? (
        <MatchView
          model={buildMatchViewModel(
            home,
            away,
            simulate({ home, away, seed: SEED, targetGoalsPerMatch: rate }),
          )}
        />
      ) : null}
    </main>
  );
}
```

- [ ] **Step 2: Rewrite `/game` as the gate**

Replace `src/app/[locale]/game/page.tsx` entirely:

```tsx
import type { Metadata } from "next";
import { getTranslations, setRequestLocale } from "next-intl/server";
import { ModeGate } from "@/features/game/components/ModeGate";

// force-static like every /game/* route. The gate loads NO data — it renders from
// domain/modes.ts alone — so there is nothing here that could pull it onto a lambda.
export const dynamic = "force-static";
export const revalidate = 86400;

type Props = { params: Promise<{ locale: string }> };

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params;
  setRequestLocale(locale);
  const t = await getTranslations("game");
  return { title: t("hubTitle"), description: t("hubSubtitle") };
}

export default async function GameHubPage({ params }: Props) {
  const { locale } = await params;
  setRequestLocale(locale);

  return (
    <main className="mx-auto w-full max-w-5xl px-4 py-10">
      <ModeGate />
    </main>
  );
}
```

- [ ] **Step 3: Update the static guard's route list**

In `tests/unit/game-routes-static.test.ts`, replace the comment inside the "finds every game route" case:

```ts
it("finds every game route", () => {
  // If this drops the glob broke and every assertion below is vacuous. Raise it when
  // a route is added: /game (the gate), /game/demo, /game/chaos, /game/draft.
  // TASK-1832 removed /game/play — it is a next.config redirect now, not a route.
  expect(files.length).toBeGreaterThanOrEqual(4);
});
```

- [ ] **Step 4: Run the game suite**

Run: `pnpm test tests/unit/game-routes-static.test.ts tests/unit/game-modes.test.ts`
Expected: PASS. The registry's "points every href at a route that exists" case now sees `/game/demo` too, and `/game/draft` still resolves.

- [ ] **Step 5: Commit**

```bash
git add src/app/[locale]/game/page.tsx src/app/[locale]/game/demo/page.tsx tests/unit/game-routes-static.test.ts
git commit -m "feat(game): make /game the mode gate and move the demo to /game/demo"
```

---

## Task 6: Retire `/game/play`

**Files:**

- Delete: `src/app/[locale]/game/play/page.tsx`
- Modify: `next.config.ts`
- Modify: `tests/unit/next-config-redirects.test.ts`

- [ ] **Step 1: Write the failing redirect assertion**

Add to `tests/unit/next-config-redirects.test.ts` (the file already does
`import nextConfig, { CURRENT_SEASON_FOR_REDIRECT } from "../../next.config";` — no import
change needed):

```ts
it("redirects the retired /game/play to /game/draft in both locales", async () => {
  const redirects = (await nextConfig.redirects?.()) ?? [];
  const en = redirects.find((r) => r.source === "/game/play");
  const ar = redirects.find((r) => r.source === "/ar/game/play");

  expect(en, "English /game/play redirect missing").toBeDefined();
  expect(en!.destination).toBe("/game/draft");
  expect(en!.permanent).toBe(true);

  expect(ar, "Arabic /game/play redirect missing").toBeDefined();
  expect(ar!.destination).toBe("/ar/game/draft");
  expect(ar!.permanent).toBe(true);
});
```

- [ ] **Step 2: Run it and watch it fail**

Run: `pnpm test tests/unit/next-config-redirects.test.ts`
Expected: FAIL — "English /game/play redirect missing".

- [ ] **Step 3: Add the redirects**

In `next.config.ts`, inside the returned array of `redirects()`, add before `...sectionRedirects`:

```ts
      // TASK-1832 — /game/play was byte-identical to /game/draft. The draft route is
      // canonical; this keeps any shared or bookmarked link working.
      //
      // ⚠️ Next forwards the incoming query onto the destination and offers no way to
      // strip it. Wanted here (`?phase=` survives the hop), but it is the same behaviour
      // that caused the TASK-M71a bug — noted so nobody reads it as accidental.
      { source: "/game/play", destination: "/game/draft", permanent: true },
      { source: "/ar/game/play", destination: "/ar/game/draft", permanent: true },
```

- [ ] **Step 4: Delete the route**

```bash
git rm src/app/[locale]/game/play/page.tsx
```

The directory should be left empty and removed by git automatically.

- [ ] **Step 5: Run the tests**

Run: `pnpm test tests/unit/next-config-redirects.test.ts tests/unit/game-routes-static.test.ts`
Expected: PASS both. The static guard now finds exactly 4 routes.

- [ ] **Step 6: Commit**

```bash
git add next.config.ts tests/unit/next-config-redirects.test.ts src/app/[locale]/game/play
git commit -m "feat(game): retire /game/play in favour of a redirect to /game/draft"
```

---

## Task 7: Navigation, sitemap and route warming

**Files:**

- Modify: `src/components/layout/nav-items.ts`, `src/app/sitemap.ts`, `scripts/warm-e2e-routes.sh`
- Modify: `tests/unit/nav-items.test.ts`, `tests/unit/sitemap.test.ts`

- [ ] **Step 1: Write the failing nav + sitemap assertions**

In `tests/unit/nav-items.test.ts`, first widen the existing import — the file currently imports `NAV_ITEMS` only:

```ts
import { NAV_ITEMS, PRIMARY_NAV_HREFS } from "@/components/layout/nav-items";
```

Then add these cases:

```ts
it("puts /game in the nav and inline in the primary pills", () => {
  const game = NAV_ITEMS.find((i) => i.href === "/game");
  expect(game, "/game missing from NAV_ITEMS").toBeDefined();
  expect(game!.key).toBe("game");
  // TASK-1832 D8 — a sixth inline pill, not folded into "More ▾".
  expect(PRIMARY_NAV_HREFS).toContain("/game");
});

it("keeps /game out of the season path model", async () => {
  // `navHrefForSeason` rewrites a nav href into /seasons/<year>/<slug> when the slug is
  // a SECTION_SLUG. "game" must never be one, or viewing 2003 would link to
  // /seasons/2003/game, which does not exist.
  const { SECTION_SLUGS } = await import("@/features/seasons/section-slugs");
  expect(SECTION_SLUGS as readonly string[]).not.toContain("game");
});
```

Add to `tests/unit/sitemap.test.ts`:

```ts
it("lists /game with its Arabic alternate", async () => {
  const entries = await sitemap();
  const game = entries.find((e) => e.url.endsWith("/game"));
  expect(game, "/game missing from the sitemap").toBeDefined();
  expect(game!.alternates?.languages).toMatchObject({
    ar: expect.stringContaining("/ar/game"),
  });
});

it("keeps the game sub-routes OUT of the sitemap", async () => {
  const entries = await sitemap();
  for (const path of ["/game/draft", "/game/chaos", "/game/demo"]) {
    expect(
      entries.some((e) => e.url.endsWith(path)),
      path,
    ).toBe(false);
  }
});
```

Match the existing import style in each file (`NAV_ITEMS` / `PRIMARY_NAV_HREFS`, and however `sitemap` is imported).

- [ ] **Step 2: Run them and watch them fail**

Run: `pnpm test tests/unit/nav-items.test.ts tests/unit/sitemap.test.ts`
Expected: FAIL — "/game missing from NAV_ITEMS" and "/game missing from the sitemap".

- [ ] **Step 3: Add `/game` to the nav**

In `src/components/layout/nav-items.ts`, append to `NAV_ITEMS` (last, so it reads as a departure from the encyclopedia):

```ts
  { href: "/game", label: "Game", key: "game" },
```

and append to `PRIMARY_NAV_HREFS`:

```ts
  "/game",
```

Update the comment above `PRIMARY_NAV_HREFS` to note that `/game` is the sixth pill and is accented (TASK-1832 D8).

- [ ] **Step 4: Add `/game` to the sitemap**

In `src/app/sitemap.ts`, add to `staticRoutes`:

```ts
    // TASK-1832 — the game hub. Only the gate is indexable; the mode sub-routes are app
    // surfaces, not content, and stay out.
    { url: `${base}/game`, alternates: langs("/game"), changeFrequency: "monthly", priority: 0.6 },
```

- [ ] **Step 5: Accent the game pill**

In `src/components/layout/PrimaryNav.tsx`, inside the `PRIMARY.map` callback, give `/game` its accent. Replace the `className` on the `<Link>` with:

```tsx
              className={cn(
                "ix-glow rounded-md px-3 py-1.5 text-sm font-medium",
                active
                  ? "bg-background text-primary shadow-sm"
                  : "text-muted-foreground hover:text-foreground",
                // TASK-1832 D8 — the game is the one destination that is not the
                // encyclopedia, so it reads as a departure rather than another data page.
                item.href === "/game" && !active && "text-primary font-semibold",
              )}
```

- [ ] **Step 6: Update the warm-routes script**

In `scripts/warm-e2e-routes.sh`, in the `ROUTES` array: replace the `"/game/play"` line with `"/game/demo"`. Leave `"/game"`, `"/game/chaos"` and `"/game/draft"` in place.

- [ ] **Step 7: Run the affected suites**

Run: `pnpm test tests/unit/nav-items.test.ts tests/unit/sitemap.test.ts tests/unit/primary-nav.test.tsx tests/unit/mobile-nav.test.tsx`
Expected: PASS.

`nav-items.test.ts` asserts no total count — it uses `arrayContaining` plus adjacency
checks (Managers after Teams, Players after Managers, Leaderboards after Players), so
appending `/game` **last** breaks none of them. That is why the item goes at the end rather
than mid-list. `primary-nav.test.tsx` and `mobile-nav.test.tsx` render from `NAV_ITEMS`; if
either asserts a rendered link count, raise it by one and cite TASK-1832 in the diff.

- [ ] **Step 8: Verify the nav fits at 1024px**

Start the dev server and check the header at a 1024px-wide viewport — six pills plus "More ▾" is tighter than the previous five.

```bash
pnpm dev
```

Expected: no wrapping or overflow in the pill row. **If it overflows**, stop and report it — the spec's fallback (D8 option B) is moving `/compare` out of `PRIMARY_NAV_HREFS`, but that is an owner decision, not yours.

- [ ] **Step 9: Commit**

```bash
git add src/components/layout/nav-items.ts src/components/layout/PrimaryNav.tsx src/app/sitemap.ts scripts/warm-e2e-routes.sh tests/unit/nav-items.test.ts tests/unit/sitemap.test.ts
git commit -m "feat(game): put the game hub in the header nav and the sitemap"
```

---

## Task 8: The pre-match screen

**Files:**

- Create: `src/features/game/components/MiniPitch.tsx`
- Modify: `src/features/game/components/MatchupPreview.tsx`, `src/features/game/components/GamePlay.tsx`
- Test: `tests/unit/game-matchup-preview.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
import { screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import { formationByName } from "@/features/game/domain/formation";
import { makeGameTeam } from "@/features/game/domain/team";
import type { GamePlayer } from "@/features/game/domain/player";
import { renderWithIntl } from "./_helpers/intl";

const { MatchupPreview } = await import("@/features/game/components/MatchupPreview");

const formation = formationByName("4-4-2 Flat");

function team(name: string, prefix: string) {
  const players = formation.slots.map((slot, i) => ({
    playerId: 1000 + i,
    season: 2020,
    name: `${prefix}-${i}`,
    role: slot.role,
    altRoles: [],
    foot: null,
    height: null,
    provenance: null,
    ratings: null,
    club: "Club",
  })) as unknown as GamePlayer[];
  return makeGameTeam(1, name, 2020, formation, players);
}

describe("MatchupPreview", () => {
  it("shows both XIs, not just the names", () => {
    renderWithIntl(
      <MatchupPreview
        home={team("Your XI", "H")}
        away={team("Rivals", "A")}
        referee="strict"
        weather="rain"
        onKickOff={vi.fn()}
        onBack={vi.fn()}
      />,
    );

    expect(screen.getByText("Your XI")).toBeInTheDocument();
    expect(screen.getByText("Rivals")).toBeInTheDocument();
    // One player node per side.
    expect(screen.getByText("H-0")).toBeInTheDocument();
    expect(screen.getByText("A-0")).toBeInTheDocument();
    expect(screen.getAllByRole("group", { name: /line-up/i })).toHaveLength(2);
  });

  it("explains what the referee and weather DO, not just their names", () => {
    renderWithIntl(
      <MatchupPreview
        home={team("Your XI", "H")}
        away={team("Rivals", "A")}
        referee="strict"
        weather="rain"
        onKickOff={vi.fn()}
        onBack={vi.fn()}
      />,
    );

    expect(screen.getByText("Strict")).toBeInTheDocument();
    expect(screen.getByText(/books more/i)).toBeInTheDocument();
    expect(screen.getByText(/scrappier/i)).toBeInTheDocument();
  });

  it("still kicks off and still goes back", async () => {
    const onKickOff = vi.fn();
    const onBack = vi.fn();
    const user = (await import("@testing-library/user-event")).default;
    renderWithIntl(
      <MatchupPreview
        home={team("Your XI", "H")}
        away={team("Rivals", "A")}
        referee={null}
        weather={null}
        onKickOff={onKickOff}
        onBack={onBack}
      />,
    );

    await user.click(screen.getByRole("button", { name: /kick off/i }));
    expect(onKickOff).toHaveBeenCalled();
    await user.click(screen.getByRole("button", { name: /squad/i }));
    expect(onBack).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run it and watch it fail**

Run: `pnpm test tests/unit/game-matchup-preview.test.tsx`
Expected: FAIL — `MatchupPreview` still takes `homeName`/`awayName`, so the `home`/`away` props are rejected and no line-up groups render.

- [ ] **Step 3: Add the impact strings**

Add to `game` in `src/i18n/messages/en.json`:

```json
"refereeStrictImpact": "Books more. Keep your tackling clean.",
"refereeLenientImpact": "Lets it flow. A physical game goes unpunished.",
"refereeCrowdInfluencedImpact": "Leans toward the home crowd.",
"weatherClearImpact": "No effect.",
"weatherRainImpact": "Scrappier. Passing suffers.",
"weatherHeavyRainImpact": "Much scrappier. Long balls travel better.",
"weatherWindImpact": "Shots from distance become unreliable.",
"weatherSnowImpact": "Scrappier, and the ball holds up.",
"previewLineupAria": "{team} line-up",
"previewImpact": "Impact"
```

And the Arabic equivalents in `ar.json`:

```json
"refereeStrictImpact": "يشهر البطاقات كثيرًا. انتبه لتدخلاتك.",
"refereeLenientImpact": "يترك اللعب يجري. الخشونة تمر دون عقاب.",
"refereeCrowdInfluencedImpact": "يميل مع جمهور الأرض.",
"weatherClearImpact": "لا تأثير.",
"weatherRainImpact": "لعب مفكك. التمرير يتأثر.",
"weatherHeavyRainImpact": "تفكك أكبر. الكرات الطويلة أجدى.",
"weatherWindImpact": "التسديد من بعيد يصبح غير موثوق.",
"weatherSnowImpact": "لعب مفكك والكرة تتوقف.",
"previewLineupAria": "تشكيلة {team}",
"previewImpact": "التأثير"
```

- [ ] **Step 4: Write `MiniPitch`**

Create `src/features/game/components/MiniPitch.tsx`:

```tsx
"use client";
import { useTranslations } from "next-intl";
import type { GameTeam } from "@/features/game/domain/team";

/**
 * A read-only formation render for the VS screen.
 *
 * Deliberately NOT `TacticalPitch`: that one owns selection, eligibility highlighting and
 * error states, none of which mean anything once the squad is locked. A separate,
 * presentational component is smaller than the read-only mode it would otherwise need.
 */
export function MiniPitch({ team }: { team: GameTeam }) {
  const t = useTranslations("game");
  const rows = [...new Set(team.formation.slots.map((s) => s.row))].sort((a, b) => b - a);

  return (
    <div
      role="group"
      aria-label={t("previewLineupAria", { team: team.name })}
      className="rounded-xl bg-[radial-gradient(120%_80%_at_50%_-10%,#12202c,#060a0f)] p-3 ring-1 ring-cyan-400/20"
    >
      <p className="mb-2 text-center text-sm font-bold text-white">{team.name}</p>
      {rows.map((row) => (
        <div key={row} className="mb-1.5 flex justify-center gap-1.5">
          {team.formation.slots
            .map((slot, index) => ({ slot, index }))
            .filter(({ slot }) => slot.row === row)
            .map(({ slot, index }) => (
              <span
                key={`${slot.row}-${slot.col}`}
                className="rounded bg-slate-800/80 px-1.5 py-1 text-[9px] font-semibold text-slate-200"
              >
                {team.players[index]?.name ?? slot.role}
              </span>
            ))}
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 5: Rewrite `MatchupPreview`**

Replace `src/features/game/components/MatchupPreview.tsx`:

```tsx
"use client";
import { useTranslations } from "next-intl";
import type { RefereeStyle, Weather } from "@/features/game/domain/match-types";
import type { GameTeam } from "@/features/game/domain/team";
import { MiniPitch } from "./MiniPitch";

interface Props {
  home: GameTeam;
  away: GameTeam;
  /**
   * ⚠️ READ from the first segment's events, never recomputed.
   *
   * `pickReferee(rng())` and `pickWeather(rng())` are the first two draws inside
   * `runMatch`. Drawing them again out here would consume from a different stream and
   * show the coach an official who is not the one about to take charge.
   */
  referee: RefereeStyle | null;
  weather: Weather | null;
  onKickOff: () => void;
  onBack: () => void;
}

const REFEREE_KEY: Record<RefereeStyle, string> = {
  strict: "refereeStrict",
  lenient: "refereeLenient",
  "crowd-influenced": "refereeCrowdInfluenced",
};

const REFEREE_IMPACT_KEY: Record<RefereeStyle, string> = {
  strict: "refereeStrictImpact",
  lenient: "refereeLenientImpact",
  "crowd-influenced": "refereeCrowdInfluencedImpact",
};

const WEATHER_KEY: Record<Weather, string> = {
  clear: "weatherClear",
  rain: "weatherRain",
  "heavy-rain": "weatherHeavyRain",
  wind: "weatherWind",
  snow: "weatherSnow",
};

const WEATHER_IMPACT_KEY: Record<Weather, string> = {
  clear: "weatherClearImpact",
  rain: "weatherRainImpact",
  "heavy-rain": "weatherHeavyRainImpact",
  wind: "weatherWindImpact",
  snow: "weatherSnowImpact",
};

/**
 * The VS screen — both XIs, who is refereeing, and what the weather is doing.
 *
 * ⚠️ This is a PHASE, not a route (TASK-1832 D9). The live session — the generator, the
 * seed, the drafted XI — lives in `GamePlay`'s memory; navigating to `/game/pre-match`
 * would drop it, and surviving that means lifting session state into a provider for no
 * visible gain.
 *
 * Not trivia: a strict referee books far more and rain makes the game scrappier, so each
 * condition carries a line on what it does rather than only what it is.
 */
export function MatchupPreview({ home, away, referee, weather, onKickOff, onBack }: Props) {
  const t = useTranslations("game");

  return (
    <div className="mx-auto w-full max-w-3xl">
      <h1 className="text-2xl font-extrabold tracking-tight">{t("playPreviewTitle")}</h1>

      <div className="my-6 grid items-center gap-3 sm:grid-cols-[1fr_auto_1fr]">
        <MiniPitch team={home} />
        <span className="text-center font-mono text-sm font-black text-cyan-300">{"–"}</span>
        <MiniPitch team={away} />
      </div>

      <dl className="grid gap-3 sm:grid-cols-2">
        <div className="border-border rounded-lg border p-3">
          <dt className="text-muted-foreground font-mono text-[10px] font-bold tracking-widest uppercase">
            {t("playReferee")}
          </dt>
          <dd className="mt-1 text-sm font-semibold">
            {referee != null ? t(REFEREE_KEY[referee]) : "—"}
          </dd>
          {referee != null ? (
            <dd className="text-muted-foreground mt-1 text-xs">{t(REFEREE_IMPACT_KEY[referee])}</dd>
          ) : null}
        </div>
        <div className="border-border rounded-lg border p-3">
          <dt className="text-muted-foreground font-mono text-[10px] font-bold tracking-widest uppercase">
            {t("playWeather")}
          </dt>
          <dd className="mt-1 text-sm font-semibold">
            {weather != null ? t(WEATHER_KEY[weather]) : "—"}
          </dd>
          {weather != null ? (
            <dd className="text-muted-foreground mt-1 text-xs">{t(WEATHER_IMPACT_KEY[weather])}</dd>
          ) : null}
        </div>
      </dl>

      <div className="mt-6 flex items-center gap-3">
        <button
          type="button"
          onClick={onBack}
          className="border-border rounded-md border px-4 py-2 text-sm font-semibold"
        >
          {t("playBackToSquad")}
        </button>
        <button
          type="button"
          onClick={onKickOff}
          className="bg-primary text-primary-foreground ms-auto rounded-md px-5 py-2 text-sm font-bold"
        >
          {t("playKickOff")}
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 6: Update the call site**

In `src/features/game/components/GamePlay.tsx`, in the `state.phase === "preview"` branch, replace the two name props with the teams:

```tsx
<MatchupPreview
  home={match.home}
  away={match.away}
  referee={referee}
  weather={weather}
  onKickOff={() => dispatch({ type: "kickOff" })}
  onBack={() => {
    // ⚠️ Cleared in the handler, never in an effect. An effect gated on "phase is
    // not live" would race the restore effect on mount and wipe the record before
    // it could be read.
    void clearMatch();
    dispatch({ type: "backToSetup" });
  }}
/>
```

- [ ] **Step 7: Run the tests**

Run: `pnpm test tests/unit/game-matchup-preview.test.tsx tests/unit/game-play.test.tsx`
Expected: PASS. If an existing `GamePlay` test asserted the old preview markup, update it and say why in the diff.

- [ ] **Step 8: Type-check**

Run: `pnpm type-check`
Expected: clean. Vitest does not type-check — a stale `homeName` prop survives a green suite.

- [ ] **Step 9: Commit**

```bash
git add src/features/game/components/MiniPitch.tsx src/features/game/components/MatchupPreview.tsx src/features/game/components/GamePlay.tsx src/i18n/messages/en.json src/i18n/messages/ar.json tests/unit/game-matchup-preview.test.tsx
git commit -m "feat(game): show both XIs and condition impacts on the pre-match screen"
```

---

## Task 9: The end-to-end walk

**Files:**

- Create: `tests/e2e/game-hub.spec.ts`

- [ ] **Step 1: Write the spec**

```ts
// ⚠️ `test`/`expect` come from the local helper, NEVER from @playwright/test. The helper
// waits for the App Router to mount; without it a click dispatched pre-hydration is
// silently swallowed — no RSC request is issued, so no timeout value can rescue it.
import { expect, test } from "./_helpers/test";

test.describe("the game hub", () => {
  test("is reachable from the header and leads into a match", async ({ page }) => {
    await page.goto("/");

    await page
      .getByRole("navigation", { name: /primary/i })
      .getByRole("link", { name: "Game" })
      .click();
    await expect(page).toHaveURL(/\/game$/);

    await expect(page.getByRole("heading", { name: "Play PitchIQ" })).toBeVisible();

    // Locked modes are visible but are not controls.
    await expect(page.getByText("Captain's Draft")).toBeVisible();
    await expect(page.getByRole("button", { name: /Captain's Draft/ })).toHaveCount(0);

    // Mode, then format.
    await page.getByRole("button", { name: /Tactical H2H/ }).click();
    await page.getByRole("link", { name: /One Match/ }).click();
    await expect(page).toHaveURL(/\/game\/draft$/);
  });

  test("redirects the retired /game/play to the draft", async ({ page }) => {
    await page.goto("/game/play");
    await expect(page).toHaveURL(/\/game\/draft$/);
  });
});
```

- [ ] **Step 2: Run it**

Run: `pnpm test:e2e tests/e2e/game-hub.spec.ts`
Expected: PASS, 2/2. If the primary-nav accessible name differs, read it from `messages/en.json` `controls.primaryNav` and match it.

- [ ] **Step 3: Commit**

```bash
git add tests/e2e/game-hub.spec.ts
git commit -m "test(game): walk the header into the hub and into a match"
```

---

## Task 10: Full verification and docs

**Files:**

- Modify: `CLAUDE.md`, `TASKS.md`

- [ ] **Step 1: Run everything**

```bash
pnpm test
pnpm type-check
CI=true pnpm lint
```

Expected: all green. Record the new suite total — it was 1,903 before this work.

- [ ] **Step 2: Prove every game route still prerenders**

```bash
pnpm build
```

Expected: the build output marks `/[locale]/game`, `/[locale]/game/demo`, `/[locale]/game/chaos` and `/[locale]/game/draft` with `●` (prerendered) in **both** locales — not `ƒ`. A `ƒ` here is the exact regression that caused the 2026-07 Vercel Active-CPU pause. `/game/play` should not appear at all.

- [ ] **Step 3: Document the gate in CLAUDE.md**

In the `features/game/` section of `CLAUDE.md`, add a bullet after the `force-static` one:

```markdown
- **The mode roster is data.** `domain/modes.ts` is the single registry behind `/game` — every mode's group, route and per-format status (`live` / `planned`). The gate renders from it and never branches on a mode's identity, so unlocking a mode is a data change, not a component change. Locked modes render as **non-focusable** elements, never disabled buttons — eleven disabled tiles would be eleven dead tab stops.
- **`/game` is the gate, `/game/demo` is the broadcast showcase, `/game/draft` is the canonical loop.** `/game/play` was byte-identical to `/game/draft` and is now a `next.config.ts` redirect. Pre-match is a **phase**, not a route (the session generator lives in `GamePlay`'s memory).
```

- [ ] **Step 4: Flip the ticket**

In `TASKS.md`, change the TASK-1832 row from `📋 Backlog` to `✅ Done`, change the heading line to `✅ Done (2026-08-13)`, and append a shipped note recording: the final suite count, that all four routes still prerender `●` in both locales, anything the 1024px nav check turned up, and any decision you had to take that the spec did not cover.

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md TASKS.md
git commit -m "docs(game): document the mode registry and close TASK-1832"
```

- [ ] **Step 6: Push and open the PR**

```bash
git push -u origin feat/task-1832-game-hub
```

Then open the PR against `main` via the REST helper (`gh` is not available in WSL — the token is in `~/.git-credentials`). **Write the PR title out explicitly; do not derive it from a previous PR's script.**

Title: `feat(game): /game becomes the mode-selection gate (TASK-1832)`

Wait for all three CI gates to go green, then squash-merge. **Never push to `main` directly.**

---

## Self-review

**Spec coverage.** Every section maps to a task:

| Spec                                  | Task                                                                        |
| ------------------------------------- | --------------------------------------------------------------------------- |
| D1 hub-only scope                     | The whole plan; 1812 is untouched                                           |
| D2 H2H = existing loop                | Task 1 (`href: "/game/draft"`), Task 2 (name)                               |
| D3 locked modes visible               | Task 3 (inert tile), Task 4 (grouped strips)                                |
| D4 Play now / Coming soon             | Task 4                                                                      |
| D5 format as a second axis            | Task 1 (`formats`), Task 3 (`FormatChoice`)                                 |
| D6 expand in place                    | Task 3 + Task 4 (`open` / `onOpen`)                                         |
| D7 Classic rename, Survival removed   | Task 1 (registry + comment), Task 2 (strings)                               |
| D8 sixth accented pill                | Task 7                                                                      |
| D9 pre-match stays a phase            | Task 8                                                                      |
| D10 skip the ritual                   | No design task exists, by design                                            |
| D11 no `?mode=` / `?format=`          | No URL-state task exists, by design                                         |
| Route map                             | Tasks 5, 6, 7                                                               |
| i18n ~40 keys × 2                     | Task 2 (+ Task 8's impact strings)                                          |
| Motion / `loading.tsx` / force-static | Task 5 (guard), Task 10 (build proof); no height animation is used anywhere |
| Testing plan                          | Tasks 1, 3, 4, 7, 8, 9                                                      |

**Placeholders:** none — every code step carries its code, every command its expected output.

**Type consistency:** `GameMode`, `ModeId`, `GameFormat`, `ModeStatus`, `ModeGroupId`, `CollectionSurface`, `isPlayable`, `modesInGroup`, `GAME_FORMATS` are defined in Task 1 and used with those exact names in Tasks 3, 4 and 7. `MatchupPreview`'s props change from `homeName`/`awayName` to `home`/`away` in Task 8, and the only call site is updated in the same task.

**Both earlier gaps are closed.** `nav-items.test.ts` was read: it imports `NAV_ITEMS` only
(Task 7 Step 1 now widens that import) and asserts no total count, so appending `/game` last
is safe. `next-config-redirects.test.ts` was read: it already imports `nextConfig` by
default and calls `(await nextConfig.redirects?.()) ?? []`, which Task 6 Step 1 now matches
exactly.

**One judgement call the executor may still hit:** Task 9's E2E scopes the nav link by
`getByRole("navigation", { name: /primary/i })`. That name comes from
`controls.primaryNav` in `messages/en.json`. If it does not match `/primary/i`, read the
actual string and use it — do **not** drop the scoping, or the locator will also match the
mobile drawer and the footer.
